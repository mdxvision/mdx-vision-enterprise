<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- MDx Vision - AR Smart Glasses Healthcare Documentation -->
    <!-- Configured for Vuzix Blade 2 and compatible AR glasses -->

    <!-- Voice Recognition (Patent Claims 1-3) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Camera for Patient ID (Patent Claims 5-7) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Network for EHR Connection (Patent Claim 9) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Vuzix-specific permissions -->
    <uses-permission android:name="com.vuzix.permission.VOICE_COMMANDS" />
    <uses-permission android:name="com.vuzix.permission.HEAD_TRACKING" />

    <!-- Bluetooth for peripherals -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Wake lock for continuous listening -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- System overlay for HUD display -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service for persistent HUD -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- AR Glasses Hardware Features -->
    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_camera"
        android:roundIcon="@android:drawable/ic_menu_camera"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:screenOrientation="landscape">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Vuzix Voice Command Intent -->
            <intent-filter>
                <action android:name="com.vuzix.intent.action.VOICE_COMMAND" />
            </intent-filter>
        </activity>

        <!-- Barcode Scanner Activity (Patent Claims 5-7) -->
        <activity
            android:name=".BarcodeScannerActivity"
            android:label="Scan Wristband"
            android:screenOrientation="landscape"
            android:exported="false" />

        <!-- Image Capture Activity (Feature #70 - Medical Image Recognition) -->
        <activity
            android:name=".ImageCaptureActivity"
            android:label="Capture Image"
            android:screenOrientation="landscape"
            android:exported="false" />

        <!-- QR Pairing Activity (Device Authentication) -->
        <activity
            android:name=".QrPairingActivity"
            android:label="Pair Device"
            android:screenOrientation="landscape"
            android:exported="false" />

        <!-- Drone Voice Control Activity (Feature-flagged, default OFF) -->
        <activity
            android:name=".drone.DroneControlActivity"
            android:label="Drone Control"
            android:screenOrientation="landscape"
            android:exported="false" />

        <!-- Vuzix HUD Overlay Service (Feature #73) -->
        <service
            android:name=".VuzixHudService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ar_hud_overlay" />
        </service>

    </application>
</manifest>
